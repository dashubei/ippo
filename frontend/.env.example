# frontend/.env.local にコピーして値を設定してください（.env.local は .gitignore 済み）
#   cp .env.example .env.local

# バックエンド API のベースURL（未設定時の既定値: http://localhost:8000/api）
VITE_API_BASE_URL=http://localhost:8000/api
